{
  "name": "shootr-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && mkdir -p dist/server && cp server/index.js dist/server/index.js",
    "build:testflight": "VITE_SHOOTR_ENV=testflight VITE_SHOOTR_DEMO_MODE=false vite build && mkdir -p dist/server && cp server/index.js dist/server/index.js",
    "dev": "vite --host 0.0.0.0",
    "ios:build": "vite build && mkdir -p dist/server && cp server/index.js dist/server/index.js && node scripts/clean-macos-xattrs.mjs && node scripts/clean-ios-public.mjs && cap sync ios && node scripts/prepare-ios-entry.mjs && node scripts/clean-macos-xattrs.mjs",
    "ios:device-check": "xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Release -destination generic/platform=iOS -derivedDataPath ios/DerivedData/physical-readiness CODE_SIGNING_ALLOWED=NO build",
    "ios:open": "cap open ios",
    "ios:sync": "vite build && mkdir -p dist/server && cp server/index.js dist/server/index.js && node scripts/clean-macos-xattrs.mjs && node scripts/clean-ios-public.mjs && cap sync ios && node scripts/prepare-ios-entry.mjs && node scripts/clean-macos-xattrs.mjs",
    "ios:sync:testflight": "VITE_SHOOTR_ENV=testflight VITE_SHOOTR_DEMO_MODE=false vite build && mkdir -p dist/server && cp server/index.js dist/server/index.js && node scripts/clean-macos-xattrs.mjs && node scripts/clean-ios-public.mjs && cap sync ios && node scripts/prepare-ios-entry.mjs && node scripts/clean-macos-xattrs.mjs"
  },
  "dependencies": {
    "@capacitor/app": "8.1.1",
    "@capacitor/browser": "8.0.4",
    "@capacitor/cli": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/ios": "8.4.2",
    "@capacitor/keyboard": "8.0.5",
    "@capacitor/status-bar": "8.0.3",
    "@fontsource/poppins": "^5.3.0",
    "@vitejs/plugin-legacy": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "8.1.5"
  }
}
